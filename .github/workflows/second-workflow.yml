name: second-workflow
on: 
    issues: 
        types: opened
jobs:
    issues-info:
        runs-on: ubuntu-latest
        steps:
            - name: Output Context
              run: echo "${{toJson(github.event.issue)}}"  
